# Core Scientific Stack
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Interactive / Notebooks
jupyterlab>=4.0.0
ipywidgets>=8.0.0
tqdm>=4.65.0

# HANK / Macro Modeling
# Note: creating a dedicated conda env is often safer for JAX/Torch, but listing here for completeness.
econpizza>=0.1.0        # Boehl's nonlinear solver
sequence-jacobian>=1.0.0 # Auclert et al. SSJ
jax>=0.4.0              # Required for high-performance AD (used by newer SSJ/pizza versions)
jaxlib>=0.4.0

# Optional: Deep Learning (D7)
torch>=2.0.0            # PyTorch for NN surrogates (optional)

# Utilities
pyyaml>=6.0
black>=23.0.0           # Code formatting
pytest>=7.0.0           # Testing
